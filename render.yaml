services:
  - type: web
    name: guestbook-backend
    env: node
    rootDir: backend-api
    buildCommand: npm install && npm run build
    startCommand: npm run start:prod
    envVars:
      - key: SUPABASE_URL
        sync: false
      - key: SUPABASE_KEY
        sync: false
